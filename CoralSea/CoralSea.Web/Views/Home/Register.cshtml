@model CoralSea.Web.Models.RegisterViewModel
@{
    ViewBag.Title = "注册";
}

<h2>@ViewBag.Title。</h2>

@using (Html.BeginForm("Register", "Home", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    <h4>创建新帐户。</h4>
    <hr />
    <div class="form-group">
        <label class="col-md-2 control-label" for="">身份证</label>
        <div class="col-md-10">
            <input class="form-control" id="Identify" name="Identify" type="text" value="">

        </div>
    </div>
    <div class="form-group">
        <label class="col-md-2 control-label" for="">工号</label>
        <div class="col-md-10">
            <input class="form-control" id="jobNumber" name="jobNumber" type="text" value="">
        </div>
    </div>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" class="btn btn-default" value="注册" />
                    </div>
                </div>
}

